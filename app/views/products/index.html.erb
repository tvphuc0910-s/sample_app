<p style="color: green"><%= notice %></p>

<h1>Products</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <%= render(@products) || "No product" %>
  </tbody>
</table>

<%= link_to "New product", new_product_path %>
